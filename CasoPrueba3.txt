/* Esta es una función */
function suma() {
  const a = 1
  const b = 2
  const c = 3

  return ( a + b + c )
}

/* Estas son dos listas*/
let lista1 = [1, 2, 3, 4, 5]
let lista2 = [6, 7, 8, 9, 10]

/* Este es un string*/
var miString = "Ejemplo de String"

/* Este es un FOR */ 
for (const elemento in lista1 ) {
  return ( elemento * 16 / 12.5 + 10e2 )
}